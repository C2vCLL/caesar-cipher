# caesar-cipher
This project is a basic caesar cipher encoder/decoder.
It works by asking the user if they are encoding or decoding then, having the user input an message to either encode or decode.
Next it asks for the number of shifts, after it shifts the message corresponding to the shifts given.
How to run: download the file and run it with Python 3 in your terminal using "python caesar_cipher.py"
